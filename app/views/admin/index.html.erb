<p id="notice"><%= notice %></p>

<h1>Listing Vendedor</h1>

<table>
  <thead>
    <tr>
      <th>ID</th>
      <th>email</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @user.each do |user| %>
      <tr>
        <td><%= user.id %></td>
        <td><%= user.email %></td>
      <td><%= link_to 'Editar', edit_user_registration_path(user), method: :get, class:"btn btn-success btn-xs" %></td>

      </tr>

    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New User', new_user_registration_path %>
